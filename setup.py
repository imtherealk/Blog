import setuptools
from setuptools import setup

setup(
    name='realk',
    version='',
    packages=setuptools.find_packages(),
    install_requires=['django', 'pillow'],
    url='',
    license='',
    author='my',
    author_email='',
    description=''
)
